# 020. 有效的括号

> 难度：🟢 Easy | 标签：`栈` `字符串`

## 题目描述

给定一个只包括 `'('`，`')'`，`'{'`，`'}'`，`'['`，`']'` 的字符串 `s`，判断字符串是否有效。

有效字符串需满足：
1. 左括号必须用相同类型的右括号闭合
2. 左括号必须以正确的顺序闭合
3. 每个右括号都有一个对应的相同类型的左括号

## 示例

```
输入：s = "()"       输出：true
输入：s = "()[]{}"   输出：true
输入：s = "(]"       输出：false
输入：s = "([)]"     输出：false
输入：s = "{[]}"     输出：true
```

## 解题思路

### 栈 O(n) ⭐

遍历字符串：
- 遇到左括号 `(` `{` `[`：入栈
- 遇到右括号：检查栈顶是否是对应的左括号
  - 匹配：弹出栈顶
  - 不匹配或栈为空：返回 `false`
- 遍历结束后：栈为空则有效（所有括号都已匹配）

## 代码

```java
public boolean isValid(String s) {
    Deque<Character> stack = new ArrayDeque<>();
    
    for (char c : s.toCharArray()) {
        if (c == '(' || c == '{' || c == '[') {
            stack.push(c); // 左括号入栈
        } else {
            // 右括号：检查是否匹配
            if (stack.isEmpty()) return false;
            char top = stack.pop();
            if (c == ')' && top != '(') return false;
            if (c == '}' && top != '{') return false;
            if (c == ']' && top != '[') return false;
        }
    }
    return stack.isEmpty(); // 栈为空才有效
}

// 更简洁的写法：遇到右括号时直接压入对应的左括号期望
public boolean isValid2(String s) {
    Deque<Character> stack = new ArrayDeque<>();
    
    for (char c : s.toCharArray()) {
        if (c == '(') stack.push(')');
        else if (c == '{') stack.push('}');
        else if (c == '[') stack.push(']');
        else if (stack.isEmpty() || stack.pop() != c) return false;
    }
    return stack.isEmpty();
}
```

## 复杂度分析

| | 复杂度 |
|---|---|
| 时间 | O(n) |
| 空间 | O(n)，最坏情况全是左括号 |

## 相关题目

- [[032. 最长有效括号]] — Hard，求最长有效子串
- [[022. 括号生成]] — 回溯生成所有合法括号
- [[155. 最小栈]] — 支持获取最小值的栈
