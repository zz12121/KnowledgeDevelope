# 104. 二叉树的最大深度

> 难度：🟢 Easy | 标签：`树` `DFS` `BFS` `递归`

## 题目描述

给定一个二叉树 `root`，返回其最大深度。

二叉树的**最大深度**是指从根节点到最远叶子节点的最长路径上的节点数。

## 示例

```
输入：root = [3, 9, 20, null, null, 15, 7]
        3
       / \
      9  20
        /  \
       15   7

输出：3
```

## 解题思路

### 方法一：DFS 递归 ⭐

树的最大深度 = `max(左子树深度, 右子树深度) + 1`

递归的底：`null` 节点深度为 0

### 方法二：BFS 迭代

层序遍历，遍历完所有层后，层数即为最大深度。

## 代码

```java
// 方法一：DFS 递归（推荐，代码最简洁）
public int maxDepth(TreeNode root) {
    if (root == null) return 0; // 空节点深度为 0
    int leftDepth = maxDepth(root.left);
    int rightDepth = maxDepth(root.right);
    return Math.max(leftDepth, rightDepth) + 1;
}

// 一行版
public int maxDepth_oneLine(TreeNode root) {
    return root == null ? 0 : Math.max(maxDepth(root.left), maxDepth(root.right)) + 1;
}

// 方法二：BFS 迭代
public int maxDepth_bfs(TreeNode root) {
    if (root == null) return 0;
    Queue<TreeNode> queue = new LinkedList<>();
    queue.offer(root);
    int depth = 0;
    
    while (!queue.isEmpty()) {
        depth++;
        int size = queue.size();
        for (int i = 0; i < size; i++) {
            TreeNode node = queue.poll();
            if (node.left != null) queue.offer(node.left);
            if (node.right != null) queue.offer(node.right);
        }
    }
    return depth;
}
```

## 复杂度分析

| 方法 | 时间 | 空间 |
|------|------|------|
| DFS 递归 | O(n) | O(h)，h 为树高，最坏 O(n) |
| BFS 迭代 | O(n) | O(w)，w 为最宽的一层 |

## 相关题目

- [[111. 二叉树的最小深度]] — 最近叶子节点的深度
- [[543. 二叉树的直径]] — 任意两节点间最长路径
- [[110. 平衡二叉树]] — 判断是否平衡（左右子树深度差 ≤ 1）
