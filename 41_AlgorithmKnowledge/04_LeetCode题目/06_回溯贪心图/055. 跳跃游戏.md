# 055. 跳跃游戏

> 难度：🟡 Medium | 标签：`贪心` `数组` `动态规划`

## 题目描述

给你一个非负整数数组 `nums`，你最初位于数组的**第一个下标**。数组中的每个元素代表你在该位置可以跳跃的**最大长度**。

判断你是否能够到达最后一个下标，如果可以，返回 `true`；否则，返回 `false`。

## 示例

```
输入：nums = [2, 3, 1, 1, 4]
输出：true
解释：可以先跳 1 步到下标 1，然后跳 3 步到最后一个下标

输入：nums = [3, 2, 1, 0, 4]
输出：false
解释：无论如何都会到达下标 3，但该下标的最大跳跃长度为 0，不能到达最后一个下标
```

## 解题思路

### 贪心：维护最远可达距离 ⭐

维护变量 `maxReach`：当前能到达的最远下标。

遍历数组，若当前下标 `i > maxReach`，说明无法到达 `i`，返回 false。
否则更新 `maxReach = max(maxReach, i + nums[i])`。

## 代码

```java
// 方法一：贪心 O(n) ⭐
public boolean canJump(int[] nums) {
    int maxReach = 0; // 当前能到达的最远位置
    
    for (int i = 0; i < nums.length; i++) {
        if (i > maxReach) return false; // 当前位置不可达
        maxReach = Math.max(maxReach, i + nums[i]); // 更新最远可达
    }
    return true;
}

// 方法二：动态规划 O(n²)（理解用）
public boolean canJump_dp(int[] nums) {
    int n = nums.length;
    boolean[] dp = new boolean[n];
    dp[0] = true;
    
    for (int i = 1; i < n; i++) {
        for (int j = 0; j < i; j++) {
            if (dp[j] && j + nums[j] >= i) {
                dp[i] = true;
                break;
            }
        }
    }
    return dp[n - 1];
}
```

## 复杂度分析

| 方法 | 时间 | 空间 |
|------|------|------|
| 贪心 | O(n) | O(1) |
| DP | O(n²) | O(n) |

## 相关题目

- [[045. 跳跃游戏 II]] — 求最少跳跃次数（也是贪心）
- [[1306. 跳跃游戏 III]] — 可以向左或向右跳，判断能否到达值为0的位置
