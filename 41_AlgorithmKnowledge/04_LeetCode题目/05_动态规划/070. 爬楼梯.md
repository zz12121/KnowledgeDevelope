# 070. 爬楼梯

> 难度：🟢 Easy | 标签：`动态规划` `记忆化搜索` `数学`

## 题目描述

假设你正在爬楼梯，需要 `n` 阶才能到达楼顶。

每次你可以爬 `1` 或 `2` 个台阶，你有多少种不同的方法可以爬到楼顶？

## 示例

```
输入：n = 2
输出：2
解释：有两种方法：1+1，2

输入：n = 3
输出：3
解释：有三种方法：1+1+1，1+2，2+1
```

## 解题思路

到达第 n 阶，可以从：
- 第 n-1 阶爬 1 步
- 第 n-2 阶爬 2 步

因此 `f(n) = f(n-1) + f(n-2)`，本质上是**斐波那契数列**！

## 代码

```java
// 方法一：动态规划（数组）
public int climbStairs(int n) {
    if (n <= 2) return n;
    int[] dp = new int[n + 1];
    dp[1] = 1;
    dp[2] = 2;
    for (int i = 3; i <= n; i++) {
        dp[i] = dp[i - 1] + dp[i - 2];
    }
    return dp[n];
}

// 方法二：空间优化（滚动变量）⭐
public int climbStairs(int n) {
    if (n <= 2) return n;
    int prev2 = 1, prev1 = 2;
    for (int i = 3; i <= n; i++) {
        int curr = prev1 + prev2;
        prev2 = prev1;
        prev1 = curr;
    }
    return prev1;
}
```

## 复杂度分析

| | 复杂度 |
|---|---|
| 时间 | O(n) |
| 空间 | O(1)（优化版） |

## 进阶：每次可以爬 1 到 m 步

```java
public int climbStairs_m(int n, int m) {
    int[] dp = new int[n + 1];
    dp[0] = 1;
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= Math.min(m, i); j++) {
            dp[i] += dp[i - j];
        }
    }
    return dp[n];
}
```

## 相关题目

- [[509. 斐波那契数]] — 本题的直接原型
- [[198. 打家劫舍]] — 同类线性DP
- [[322. 零钱兑换]] — 完全背包（每步可选多种面值）
