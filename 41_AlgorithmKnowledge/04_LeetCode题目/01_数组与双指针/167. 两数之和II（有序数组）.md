# 167. 两数之和 II（输入有序数组）

> 难度：🟡 Medium | 标签：`数组` `双指针` `二分查找`

## 题目描述

给你一个下标从 **1** 开始的整数数组 `numbers`，该数组已按**非递减顺序**排列。请你从数组中找出满足相加之和等于目标数 `target` 的两个数。

返回两个数的下标 `[index1, index2]`（**下标从 1 开始**），满足 `1 <= index1 < index2 <= numbers.length`。

**题目数据保证每种输入只存在唯一的答案，且不可以重复使用相同的元素。**

## 示例

```
输入：numbers = [2, 7, 11, 15], target = 9
输出：[1, 2]
解释：2 + 7 = 9，返回下标 [1, 2]

输入：numbers = [2, 3, 4], target = 6
输出：[1, 3]

输入：numbers = [-1, 0], target = -1
输出：[1, 2]
```

## 解题思路

### 对撞双指针 O(n) ⭐

利用数组**有序**的性质：
- `left` 从左端，`right` 从右端
- `sum < target`：需要增大，`left++`
- `sum > target`：需要减小，`right--`
- `sum == target`：找到答案

## 代码

```java
public int[] twoSum(int[] numbers, int target) {
    int left = 0, right = numbers.length - 1;
    
    while (left < right) {
        int sum = numbers[left] + numbers[right];
        if (sum == target) {
            return new int[]{left + 1, right + 1}; // 下标从1开始
        } else if (sum < target) {
            left++;
        } else {
            right--;
        }
    }
    return new int[]{}; // 题目保证有解，不会到这里
}
```

## 复杂度分析

| | 复杂度 |
|---|---|
| 时间 | O(n) |
| 空间 | O(1) |

## 相关题目

- [[001. 两数之和]] — 无序版本，用哈希表
- [[015. 三数之和]] — 三个数的情况
