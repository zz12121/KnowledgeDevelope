# 011. 盛最多水的容器

> 难度：🟡 Medium | 标签：`数组` `双指针` `贪心`

## 题目描述

给定一个长度为 `n` 的整数数组 `height`。有 `n` 条垂线，第 `i` 条线的两个端点是 `(i, 0)` 和 `(i, height[i])`。

找出其中的两条线，使得它们与 x 轴共同构成的容器可以容纳最多的水。

返回容器可以储存的最大水量。

## 示例

```
输入：height = [1, 8, 6, 2, 5, 4, 8, 3, 7]
输出：49
解释：选取下标 1 和 8 的两条线（高度8和7），容积 = min(8,7) * (8-1) = 7 * 7 = 49
```

## 解题思路

### 双指针（贪心）O(n) ⭐

- 初始两指针指向数组两端
- 面积 = `min(height[left], height[right]) × (right - left)`
- **关键**：每次移动较**矮**的那侧指针

**为什么移动较矮的一侧？**
若移动较高的一侧：宽度减小，高度取决于另一侧（不会增加），面积只会变小或不变。
若移动较矮的一侧：宽度减小，但高度有可能增大，存在面积变大的可能。

## 代码

```java
public int maxArea(int[] height) {
    int left = 0, right = height.length - 1;
    int maxWater = 0;
    
    while (left < right) {
        // 计算当前面积
        int water = Math.min(height[left], height[right]) * (right - left);
        maxWater = Math.max(maxWater, water);
        
        // 移动较矮的一侧
        if (height[left] < height[right]) {
            left++;
        } else {
            right--;
        }
    }
    return maxWater;
}
```

## 复杂度分析

| | 复杂度 |
|---|---|
| 时间 | O(n)，只需一次遍历 |
| 空间 | O(1) |

## 相关题目

- [[042. 接雨水]] — 更难的版本，每个位置都能接水
