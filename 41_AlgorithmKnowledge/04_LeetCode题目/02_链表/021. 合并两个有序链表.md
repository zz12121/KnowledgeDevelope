# 021. 合并两个有序链表

> 难度：🟢 Easy | 标签：`链表` `递归`

## 题目描述

将两个升序链表合并为一个新的**升序**链表并返回。新链表是通过拼接给定的两个链表的所有节点组成的。

## 示例

```
输入：list1 = [1, 2, 4], list2 = [1, 3, 4]
输出：[1, 1, 2, 3, 4, 4]

输入：list1 = [], list2 = []
输出：[]

输入：list1 = [], list2 = [0]
输出：[0]
```

## 解题思路

### 方法一：迭代（哑结点） ⭐

用**虚拟头节点**（dummy）简化边界处理：
1. 比较 `list1` 和 `list2` 当前节点
2. 将较小的节点接到结果链表
3. 指针向前移动
4. 最后将剩余链表接上

### 方法二：递归

定义：合并后的链表头是两个链表头中较小的那个，其后面接上剩余部分的合并结果。

## 代码

```java
// 方法一：迭代（推荐）
public ListNode mergeTwoLists(ListNode list1, ListNode list2) {
    ListNode dummy = new ListNode(-1); // 哑节点
    ListNode curr = dummy;
    
    while (list1 != null && list2 != null) {
        if (list1.val <= list2.val) {
            curr.next = list1;
            list1 = list1.next;
        } else {
            curr.next = list2;
            list2 = list2.next;
        }
        curr = curr.next;
    }
    // 将剩余部分直接接上
    curr.next = (list1 != null) ? list1 : list2;
    
    return dummy.next;
}

// 方法二：递归
public ListNode mergeTwoLists(ListNode list1, ListNode list2) {
    if (list1 == null) return list2;
    if (list2 == null) return list1;
    
    if (list1.val <= list2.val) {
        list1.next = mergeTwoLists(list1.next, list2);
        return list1;
    } else {
        list2.next = mergeTwoLists(list1, list2.next);
        return list2;
    }
}
```

## 复杂度分析

| 方法 | 时间 | 空间 |
|------|------|------|
| 迭代 | O(m+n) | O(1) ✅ |
| 递归 | O(m+n) | O(m+n) 递归栈 |

## 相关题目

- [[023. 合并K个升序链表]] — Hard，合并多个链表（优先队列）
- [[088. 合并两个有序数组]] — 数组版本
