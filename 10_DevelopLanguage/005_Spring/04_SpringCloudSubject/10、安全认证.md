###### 1. 微服务架构中的安全问题有哪些？
微服务架构在带来敏捷性、可扩展性等优势的同时，也引入了传统单体应用所不具备的复杂安全挑战。这些安全问题可归结为**三大核心风险**：
- **数据泄露风险**：这是微服务架构下最显著的风险。由于服务被拆解，内部API数量激增，**攻击面显著扩大**。风险主要来源于：应用自身的API漏洞；密钥管理不规范（如将密钥硬编码在代码中）；服务间通信未加密或加密强度不足，导致传输中的数据被窃取。
- **未授权访问风险**：微服务的动态性和数量之多使得权限配置变得极其复杂。服务本身存在的未授权访问漏洞，或访问权限配置错误，可能被攻击者利用横向渗透整个系统。
- **拒绝服务（DoS）风险**：攻击者可以利用应用漏洞（如ReDoS）或通过CC攻击等方式，使某个关键服务的资源耗尽，从而导致系统级联故障。
**主要攻击面与防护**：API网关（WAF、限流、认证）、服务间通信（mTLS双向TLS加密）、配置中心（配置加密、最小权限访问控制）、认证授权服务（JWT签名验证、OAuth 2.0安全流程）。

📖 [[../../../../27_SpringCloudKnowledge/07_安全与分布式事务/01、安全认证与分布式事务#一、微服务安全]]

###### 2. 什么是 OAuth 2.0？
OAuth 2.0 是一个**行业标准的授权协议（RFC 6749）**，其核心目标是**解决第三方应用在不需要获取用户密码的情况下，获得对用户资源的有限访问权限的问题**。
简单来说，它解决了"**授权**"问题。例如，你允许一个微信扫码登录的网站获取你的微信头像和昵称，但决不允许它知道你的微信密码。OAuth 2.0 通过引入一个**授权层（Authorization Layer）** 和**访问令牌（Access Token）** 来实现这一机制。
**核心角色包括**：
- **资源所有者 (Resource Owner)**：通常是终端用户。
- **客户端 (Client)**：希望访问用户资源的第三方应用。
- **授权服务器 (Authorization Server)**：验证用户身份并颁发令牌的服务器（例如微信的OAuth服务器）。
- **资源服务器 (Resource Server)**：存放用户资源的服务器（例如存放微信头像的服务器）。

📖 [[../../../../27_SpringCloudKnowledge/07_安全与分布式事务/01、安全认证与分布式事务#二、OAuth 2.0]]

###### 3. OAuth 2.0 的授权模式有哪些？
OAuth 2.0 提供了多种授权模式以适应不同的应用场景：
1. **授权码模式 (Authorization Code Grant)**：这是**最常用、最安全**的模式，适用于有后端的Web应用。流程分为两步：先通过前端频道获取一个授权码，再通过安全的后端频道用授权码换取Access Token。这避免了令牌暴露给前端浏览器。
2. **隐式模式 (Implicit Grant)**：适用于纯前端应用（如单页应用SPA），但**安全性较低**。授权服务器直接将Access Token返回给前端。此模式已被PKCE扩展的授权码模式所取代，不推荐在新项目中使用。
3. **密码模式 (Resource Owner Password Credentials Grant)**：用户直接将用户名和密码交给客户端，客户端用它来换取Token。这**仅适用于高度信任的客户端**（例如同一公司内部的应用），因为用户需要向第三方暴露密码，违背OAuth初衷。
4. **客户端凭证模式 (Client Credentials Grant)**：适用于**机器对机器（M2M）的认证**，没有用户参与。客户端使用自己的身份（Client ID和Secret）直接向授权服务器申请Token，用于访问受保护的自身资源或后端API。
5. **刷新令牌 (Refresh Token)**：严格来说不是一种独立的授权模式，而是一种配套机制。当Access Token过期后，客户端可以使用Refresh Token来获取新的Access Token，而无需用户再次登录，提升了用户体验。

📖 [[../../../../27_SpringCloudKnowledge/07_安全与分布式事务/01、安全认证与分布式事务#二、OAuth 2.0]]

###### 4. 什么是 JWT？
JWT是一个**开放标准（RFC 7519）**，用于在各方之间安全地传输信息作为JSON对象。这个信息是**经过数字签名**的，因此可以被验证和信任。
JWT的**核心价值在于实现无状态的认证授权**。它是一串自包含的令牌，自身就包含了所有需要的用户声明（Claims），服务器无需在会话中保存用户状态，非常适合微服务架构和RESTful API。
一个JWT令牌由三部分组成，用点（.）分隔：`Header.Payload.Signature`。
- **Header**：通常由两部分组成，令牌类型（即"JWT"）和签名算法（如HMAC SHA256或RSA）。
- **Payload**：包含声明（Claims）。声明是关于实体（通常是用户）和其他数据的语句。常见的声明有：用户ID（`sub`）、过期时间（`exp`）、签发者（`iss`）等。
- **Signature**：用于验证消息在传递过程中没有被篡改。签名部分的生成需要Base64Url编码后的Header、Payload、一个密钥（Secret）和Header中指定的签名算法。

📖 [[../../../../27_SpringCloudKnowledge/07_安全与分布式事务/01、安全认证与分布式事务#三、JWT]]

###### 5. JWT 的工作原理是什么？
JWT的工作流程是一个典型的**基于令牌的认证**流程：
1. **认证**：用户使用凭证（如用户名密码）登录。
2. **令牌签发**：认证服务器验证凭证有效后，创建一个JWT（包含用户信息并签名）返回给客户端。
3. **令牌携带**：客户端在后续请求的`Authorization`头中携带JWT（例如 `Bearer <token>`）。
4. **令牌验证**：资源服务器收到请求后，**验证JWT的签名**以确保其未被篡改，并**检查有效期**等声明。验证通过后，即认为请求已认证。
5. **授权与响应**：资源服务器从JWT的Payload中解析出用户身份和权限，进行业务逻辑处理并返回结果。
**源码层面的签名验证**（以Java `java-jwt`库为例）：
```java
// 验证JWT
try {
    Algorithm algorithm = Algorithm.HMAC256("your-secret"); // 使用相同的密钥和算法
    JWTVerifier verifier = JWT.require(algorithm)
        .withIssuer("auth-server") // 验证签发者
        .build();
    
    DecodedJWT jwt = verifier.verify("received.jwt.token"); // 验证签名和过期时间
    String userId = jwt.getSubject(); // 安全地获取用户信息
} catch (JWTVerificationException exception){
    // 签名无效或令牌过期
}
```
**关键点**：验证的核心是**签名**。服务器使用预先约定好的密钥或非对称密钥对中的公钥，按照Header声明的算法重新计算签名，并与JWT中的签名进行比对。任何对Header或Payload的篡改都会导致签名验证失败。

📖 [[../../../../27_SpringCloudKnowledge/07_安全与分布式事务/01、安全认证与分布式事务#三、JWT]]

###### 6. JWT 与 Session 的区别是什么？
这是**无状态**和**有状态**认证架构的根本区别。
JWT是**无状态**的：所有信息存储在令牌本身，服务端无需存储Session数据，天然适合分布式系统和微服务，无需考虑Session同步问题，跨域支持良好；但令牌一旦签发，在有效期内无法主动废止（除非借助黑名单机制，这又引入了状态）。
Session是**有状态**的：需要在服务器端（内存、Redis等）存储Session数据，在集群环境下需要实现Session复制或集中存储，扩展性较差，但可随时在服务端使特定Session失效，控制力强。
**选择建议**：微服务架构和原生云应用首选JWT，因其无状态特性简化了架构。对于需要**强会话控制**（如能立即踢用户下线）的传统Web应用，Session方案可能更直接。

📖 [[../../../../27_SpringCloudKnowledge/07_安全与分布式事务/01、安全认证与分布式事务#三、JWT]]

###### 7. 什么是单点登录（SSO）？
单点登录是一种**身份认证方案**，允许用户使用**同一套凭证（如用户名密码）登录多个互信的应用系统**。用户只需在**一个中心认证服务**登录一次，即可访问所有具有权限的系统，无需重复登录。
**核心思想是"一处登录，处处访问"**。它通过将认证功能从各个应用中剥离出来，集中到一个独立的认证中心来实现。这大大提升了用户体验和系统安全性（密码只在最安全的地方输入一次）。

📖 [[../../../../27_SpringCloudKnowledge/07_安全与分布式事务/01、安全认证与分布式事务#二、OAuth 2.0]]

###### 8. 说说 OAuth 2.0 与单点登录的区别？
这是一个常见的混淆点。OAuth 2.0 和 SSO 解决的是**不同但相关**的问题。
**核心区别**：OAuth 2.0 解决"**授权**"问题，即"第三方应用能否访问我在某处的资源"，发生在**不同信任域**之间（如用微信登录知乎）；SSO 解决"**认证**"问题，即"这个用户是不是我们系统内部已知的用户"，发生在**同一信任域**内部（如登录了公司内网OA后，可直接访问CRM、ERP等系统）。
**关系**：在现代身份体系中，OAuth 2.0 常被**用作实现SSO的一种手段**。例如，OpenID Connect (OIDC) 协议在OAuth 2.0之上构建了一个认证层，专门用于实现SSO。你可以理解为：OAuth 2.0 提供了授权的框架，而OIDC利用这个框架标准化了用户身份的传递，从而实现了SSO。

📖 [[../../../../27_SpringCloudKnowledge/07_安全与分布式事务/01、安全认证与分布式事务#二、OAuth 2.0]]

###### 9. 什么是 Spring Security？
Spring Security 是一个功能强大且**高度可定制**的**身份认证和访问控制框架**，是保护基于Spring的应用程序的事实标准。它的核心设计目标是**对每一层Web请求进行细粒度的安全控制**。
**核心原理：过滤器链（Filter Chain）**
Spring Security 的本质是一个由多个**过滤器（Filter）** 组成的责任链。一个HTTP请求到达应用后，会依次经过这个过滤器链的处理，最终才能到达你的`@RestController`或`@Controller`。这些过滤器各司其职：
- `SecurityContextPersistenceFilter`：在请求开始时建立`SecurityContext`（安全上下文），请求结束后清理它。
- `UsernamePasswordAuthenticationFilter`：处理表单登录（用户名/密码认证）。
- `FilterSecurityInterceptor`：**最终的决策者**，根据配置的权限规则，决定是允许访问还是抛出异常。
**核心组件**：
- `AuthenticationManager`：认证管理的入口点。
- `ProviderManager`：委托给一系列`AuthenticationProvider`来完成具体认证逻辑（如从数据库校验用户）。
- `UserDetailsService`：核心接口，用于根据用户名加载用户信息。开发者通常需要实现这个接口。

📖 [[../../../../27_SpringCloudKnowledge/07_安全与分布式事务/01、安全认证与分布式事务#一、微服务安全]]

###### 10. Spring Security 与 Shiro 的区别是什么？
简单说，**Spring Security功能更强大但学习曲线陡，Shiro更轻量更直观**。
Spring Security 与Spring生态无缝集成，是Spring Boot官方首选安全框架，功能极其丰富（从Web安全到方法级安全、OAuth 2.0、SAML等），但概念繁多、配置复杂，学习曲线陡峭，定制化开发需要对框架有较深理解。
Apache Shiro 不依赖Spring，可独立于任何容器或框架运行，API设计直观，易于理解和上手，灵活性非常高，但高级特性（如OAuth 2.0）需要插件或自行实现。
**选型建议**：对于标准的Spring Boot微服务项目，**Spring Security是更自然、更强大的选择**。如果项目非常小且技术栈简单，或团队对Spring生态不熟，Shiro的轻量级可能是一个优势。

📖 [[../../../../27_SpringCloudKnowledge/07_安全与分布式事务/01、安全认证与分布式事务#一、微服务安全]]

###### 11. 微服务架构中如何实现统一鉴权？
在微服务架构中，**统一鉴权**的主流模式是 **"网关集中认证，服务分散授权"**。
**核心模式：API网关模式**
1. **网关作为安全边界**：所有外部请求必须首先通过API网关，网关负责执行严格的认证。
2. **网关处完成认证**：对于每个进入的请求，网关执行JWT验证（检查签名、有效期、签发者），如果令牌无效或过期，直接返回`401 Unauthorized`。
3. **向下游传递用户上下文**：认证通过后，网关将解析出的用户信息（如用户ID、角色）以**透明令牌**的形式（例如放在HTTP头 `X-User-Info`中）转发给背后的微服务。切勿直接传递原始JWT。
**技术实现（Spring Cloud Gateway + Spring Security）**：
```java
@Component
public class JwtAuthenticationFilter implements GlobalFilter, Ordered {
    @Override
    public Mono<Void> filter(ServerWebExchange exchange, GatewayFilterChain chain) {
        String token = extractJwtFromRequest(exchange.getRequest());
        if (token == null) {
            exchange.getResponse().setStatusCode(HttpStatus.UNAUTHORIZED);
            return exchange.getResponse().setComplete();
        }
        try {
            Claims claims = Jwts.parser().setSigningKey(publicKey).parseClaimsJws(token).getBody();
            ServerHttpRequest mutatedRequest = exchange.getRequest().mutate()
                    .header("X-User-ID", claims.getSubject())
                    .header("X-User-Roles", (String) claims.get("roles"))
                    .build();
            return chain.filter(exchange.mutate().request(mutatedRequest).build());
        } catch (JwtException e) {
            exchange.getResponse().setStatusCode(HttpStatus.UNAUTHORIZED);
            return exchange.getResponse().setComplete();
        }
    }
}
```

📖 [[../../../../27_SpringCloudKnowledge/07_安全与分布式事务/01、安全认证与分布式事务#一、微服务安全]]

###### 12. 微服务架构中如何实现权限控制？
在统一鉴权确定了"用户是谁"之后，权限控制要解决"用户能做什么"的问题。这通常在**各个微服务内部**完成，实现细粒度的访问控制。
**核心模式：RBAC（基于角色的访问控制）**：其核心是 **User -> Role -> Permission** 的映射。权限（Permission）是资源（如API端点`/api/users`）和操作（如`GET`, `POST`）的组合。
**技术实现：Spring Security Method-Level Security**
```java
@Configuration
@EnableGlobalMethodSecurity(prePostEnabled = true) // 启用@PreAuthorize等注解
public class MethodSecurityConfig extends GlobalMethodSecurityConfiguration {}

@RestController
@RequestMapping("/api/orders")
public class OrderController {

    @DeleteMapping("/{id}")
    @PreAuthorize("hasAuthority('ORDER_DELETE') or @permissionService.isOrderOwner(authentication, #id)")
    public ResponseEntity<Void> deleteOrder(@PathVariable Long id) {
        orderService.delete(id);
        return ResponseEntity.ok().build();
    }

    @GetMapping
    @PreAuthorize("hasRole('ADMIN')")
    public List<Order> getAllOrders() {
        return orderService.findAll();
    }
}
```
**高级模式：ABAC（基于属性的访问控制）**：对于更复杂的场景，RBAC显得力不从心。此时可采用ABAC，其决策基于**主体（用户）、资源、环境、操作**的属性。
**服务网格集成**：在现代架构（如Istio服务网格）中，部分粗粒度的权限控制（如服务A能否调用服务B）可以下沉到基础设施层，通过Istio中定义`AuthorizationPolicy`，实现**零信任安全模型**。

📖 [[../../../../27_SpringCloudKnowledge/07_安全与分布式事务/01、安全认证与分布式事务#一、微服务安全]]

###### 13. Spring Security 的过滤器链是如何建立的？DelegatingFilterProxy 和 FilterChainProxy 的关系？
[[../../../../46_SecurityKnowledge/01_SpringSecurity过滤器链原理#二、核心组件详解|📖]] Spring Security 通过两层设计桥接 Servlet 容器和 Spring 容器：

**DelegatingFilterProxy（Servlet Filter）**：
- 在 Servlet 容器中注册，是 Spring Security 接入 Servlet 过滤器链的桥梁
- 自身不做安全逻辑，第一次请求时**懒加载**从 Spring ApplicationContext 中获取 `FilterChainProxy`（bean name 默认是 `springSecurityFilterChain`）
- 之后所有请求都委托给 `FilterChainProxy` 处理

**FilterChainProxy（Spring Bean）**：
- Spring Security 的真正总入口，管理多条 `SecurityFilterChain`
- 根据请求 URL 选择第一条匹配的 `SecurityFilterChain`（支持不同 URL 模式使用不同的安全配置）
- 内部通过 `VirtualFilterChain`（内部类）按顺序执行所有 Security Filter

这种两层设计的好处：`DelegatingFilterProxy` 让 Security Filter 能利用 Spring 的依赖注入；`FilterChainProxy` 支持多条安全过滤器链，不同 URL 可有完全不同的安全策略。

###### 14. Spring Security 认证流程是怎样的？UsernamePasswordAuthenticationFilter 源码？
[[../../../../46_SecurityKnowledge/01_SpringSecurity过滤器链原理#3.2 UsernamePasswordAuthenticationFilter（用户名密码认证）|📖]] 表单登录认证的完整流程：

1. **UsernamePasswordAuthenticationFilter** 拦截 `POST /login`：
   - `requiresAuthentication()` 判断是登录请求，进入认证流程
   - 提取 `username` 和 `password` 参数，封装为 `UsernamePasswordAuthenticationToken`（未认证状态）
   - 委托 `AuthenticationManager.authenticate()` 进行认证

2. **ProviderManager（AuthenticationManager 默认实现）** 遍历所有 `AuthenticationProvider`，选择能处理该 token 类型的 provider

3. **DaoAuthenticationProvider**：
   - 调用 `UserDetailsService.loadUserByUsername(username)` 加载用户（开发者实现，通常查数据库）
   - 用 `PasswordEncoder.matches()` 比对密码
   - 认证成功则返回带有 `GrantedAuthority` 的 `UsernamePasswordAuthenticationToken`（已认证状态）

4. **认证成功**：`SecurityContextHolder` 存入已认证的 `Authentication`，`SecurityContextPersistenceFilter` 在响应结束时将 `SecurityContext` 写入 Session

5. **认证失败**：`SecurityContextHolder` 清空，重定向到 `/login?error`

###### 15. AnonymousAuthenticationFilter、ExceptionTranslationFilter、FilterSecurityInterceptor 各自的作用？
[[../../../../46_SecurityKnowledge/01_SpringSecurity过滤器链原理#三、关键过滤器源码分析|📖]] 三者协同工作，构成 Spring Security 授权决策的核心：

**AnonymousAuthenticationFilter**：确保 `SecurityContextHolder` 中始终不为 null。若当前请求没有已认证的 Authentication（未登录），设置一个 `AnonymousAuthenticationToken(ROLE_ANONYMOUS)` 作为默认身份，避免后续处理时的 NPE，同时也允许根据是否是匿名用户做不同的权限判断。

**ExceptionTranslationFilter**：充当异常的"翻译官"，包裹后面的 `FilterSecurityInterceptor`。捕获两类异常：
- `AuthenticationException`（认证失败，如 token 过期） → 调用 `AuthenticationEntryPoint`（默认重定向到 `/login`）
- `AccessDeniedException`（权限不足）：若是匿名用户 → 重定向到登录页；若是已登录用户 → 返回 403（调用 `AccessDeniedHandler`）

**FilterSecurityInterceptor**：过滤器链最后的决策者：
1. 通过 `SecurityMetadataSource` 获取该请求 URL 需要的权限配置（如 `ROLE_ADMIN`）
2. 委托 `AccessDecisionManager`（默认 `AffirmativeBased`，一票通过）对当前用户的 `Authentication` 进行投票决策
3. 通过则继续执行 Servlet（进入 Spring MVC）；失败则抛出 `AccessDeniedException`（由上层的 ExceptionTranslationFilter 捕获处理）

###### 16. SecurityContextHolder 的 ThreadLocal 机制是什么？在异步场景下有什么问题？
[[../../../../46_SecurityKnowledge/01_SpringSecurity过滤器链原理#六、SecurityContextHolder 与 ThreadLocal|📖]] `SecurityContextHolder` 使用 `ThreadLocal` 存储 `SecurityContext`，使当前线程内任意位置都能直接获取登录用户信息：

```java
Authentication auth = SecurityContextHolder.getContext().getAuthentication();
String username = auth.getName();
```

**三种存储策略**（通过 `setStrategyName()` 切换）：
- `MODE_THREADLOCAL`（默认）：普通 `ThreadLocal`，仅当前线程可见
- `MODE_INHERITABLETHREADLOCAL`：`InheritableThreadLocal`，子线程继承父线程的安全上下文
- `MODE_GLOBAL`：全局单例，不推荐

**异步场景的问题**：使用 `@Async`、线程池或 `CompletableFuture` 时，任务在另一个线程执行，`ThreadLocal` 不传播，`getAuthentication()` 返回 null。

**解决方案**：配置 `DelegatingSecurityContextAsyncTaskExecutor` 包装线程池，自动将父线程的 `SecurityContext` 复制传播到异步任务线程。
