# Java 方法为什么不能重载返回值？

## 一句话说明（白话）
编译期仅靠参数区分，返回值不足以区分调用。

## 它解决什么问题 / 为什么重要
避免方法调用歧义。

## 核心原理（一步步讲清楚）
- 重载判定依据：方法名 + 参数列表。
- 返回值不参与重载决议。

##典型使用场景
- API设计。

## 简单例子 /伪代码
```java
void f(int a) {}
int f(int a) {} // 编译失败
```

## 常见坑与误区
-以为不同返回值就能重载。

##关联知识
- 重载 vs 重写

## 延伸阅读（后续补充）
- Java 方法解析
