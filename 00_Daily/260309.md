0309

0310
1.map.values() 得到的列表不支持调用add方法，会报UnsupportedOperationException
不会凭空生成一个没有键的值

2.想做一个游戏，打升级，现有的游戏规则和我老家不一样，想要弄个自己玩的
微信小程序？steam?app
使用ai完成？

根据我现在的工作经历，适合单独做的项目，感觉是后台管理平台，菜单都是动态化的，包括名称，展示出的字段也是动态化的，通用的后台管理平台？


0323
在使用SimpleDateFormat.parse(String)时，如果需要解析的字符串和日期格式不一致时，不一定会抛异常，例如 2026-03-23 格式为yyyyMMdd时，通过下标获取对应的内容，得到的会是一个奇怪的数。
Date的after方法或before方法都是不包含等于的